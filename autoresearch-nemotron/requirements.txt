# Core dependencies for NVIDIA Nemotron Model Reasoning Challenge

# PyTorch (CUDA 12.1 recommended)
torch>=2.1.0
torchvision>=0.16.0

# Transformers & PEFT for LoRA
transformers>=4.40.0
peft>=0.10.0
accelerate>=0.27.0

# Datasets for data loading
datasets>=2.18.0
dataloader>=0.14.0

# Flash Attention (optional but recommended)
# pip install flash-attn --no-build-isolation
# For CUDA 12.1:
# pip install flash-attn --no-build-isolation --no-cache-dir --torch==2.1.0 --cuda==cu121

# Memory optimization
bitsandbytes>=0.41.0  # Optional: 4-bit quantization

# Training utilities
scipy>=1.11.0
scikit-learn>=1.3.0

# Logging and monitoring
tensorboard>=2.15.0
wandb>=0.16.0  # Optional: experiment tracking

# Utilities
tqdm>=4.66.0
pyyaml>=6.0.0
regex>=2023.0.0

# Evaluation
rouge-score>=0.1.0
bleu>=1.0.0

# For data preparation
requests>=2.31.0
huggingface-hub>=0.20.0

# Development
jupyter>=1.0.0
ipykernel>=6.25.0
